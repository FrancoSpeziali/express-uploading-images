{
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.1"
  },
  "scripts": {
    "setup": "npm i && cd client && npm i",
    "build": "cd client && npm run build"
  },
  "type": "module"
}
